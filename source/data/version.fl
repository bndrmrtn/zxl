// Flare namespace
// This file is part of the Flare programming language project.
// It contains the version information for the Flare language runtime.
namespace flare;

// Using the runtime module to access the language version
use runtime;

// Define the version variable to hold the language version
const version = runtime.langVersion;
