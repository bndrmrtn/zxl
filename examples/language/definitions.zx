define Object {
    let value = 0;

    fn increment() {
        this.value = this.value + 1;
    }
}

let ob1 = Object();
let ob2 = Object();
ob2.increment();
ob1.increment();

println(ob1.value);
println(ob2.value);
