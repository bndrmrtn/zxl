namespace examples;

use application:app as app;
use core as core;

fn main() {}

let x = (10 + 4) * 3 + (5 * (2 * 3));
x = 5 + 5;

fn myApp() {
	let y = 10;
}

define UserID {
	let id = 0;

	fn construct(id) {
		this.id = id;
	}
}

let li = [1, 2];

fn x() {
	let y = 10;
}
