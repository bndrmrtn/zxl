namespace main;

define Helloer {
  fn construct() {
    println("Hello, World!");
  }

  fn sayHello() {
    println("Hello, SAY!");
  }
}

let h = Helloer();

h.sayHello();

println(h);
println(type(h));

import("other.zx");

let s = other.Sayer();

println(type(s));

s.say();