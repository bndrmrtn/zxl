namespace test;

// FEATURE IDEA
// Error blocks, with error errorVariableName: syntax
// if err != nil {} the code failed but the error remains silent
// so you can implement your own error handling logic
error err: const number = map();
if err != nil {
    println("Error:", err);
}

error err {
    const x = 1;
    x++;
}

error err: const x = 1;
if err != nil {
    println("Error:", err);
}
error err: x++;
