define MyBlock {
    let user = "Unknown";

    fn construct(name) {
        this.user = name;
        println(this.user);
    }

    fn print() {
        println(this.user);
    }
}

let block = MyBlock("Martin");

// block.print();
