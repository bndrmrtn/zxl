namespace main;

define Helloer {
  fn construct() {
    println("Hello, World!");
  }

  fn sayHello() {
    println("Hello, SAY!");
  }
}

let h = Helloer();

h.sayHello();

println(h);
println(type(h));

return;

import("other.zx");

let acc = other.val;
println(acc.$addr);