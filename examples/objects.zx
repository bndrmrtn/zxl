define Greeter {
    let name;

	fn construct(name) {
        this.name = name;
	}

	fn greet() {
	   println(this.message());
	}

	fn message() {
       return "Hello, " + this.name + "!";
    }
}

let greeter = Greeter("Martin");
greeter.greet();
